# --- core agent framework ---
langgraph>=0.2.60
langchain-core>=0.3.15
langchain-openai>=0.2.9
langchain-text-splitters>=0.3.2

# --- RAG / vector store ---
chromadb>=0.5.20
tiktoken>=0.8.0

# --- structured data / config ---
pydantic>=2.9
python-dotenv>=1.0.1
pyyaml>=6.0

# --- API layer (Phase 6) ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- MCP server (Phase 6) ---
# Pinned to the v2.x line deliberately: mcp>=1.0.0 (unpinned) would silently pick up
# whichever major version is newest at install time, and the SDK made a breaking change
# between v1.x (`from mcp.server.fastmcp import FastMCP`) and v2.x (`from mcp.server
# import MCPServer`, the class itself renamed FastMCP -> MCPServer, released alongside
# the "2026-07-28 MCP specification") - verified against the current official
# modelcontextprotocol/python-sdk README before writing mcp_server.py against it. Same
# lesson as the langgraph checkpoint security advisory: an unpinned dependency is a
# silent future break, not a convenience.
mcp[cli]>=2,<3

# --- dev / test / lint ---
pytest>=8.3
ruff>=0.7
